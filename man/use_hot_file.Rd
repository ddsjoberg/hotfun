% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/use_hot_file.R
\name{use_hot_file}
\alias{use_hot_file}
\alias{use_hot_gitignore}
\alias{use_hot_readme}
\title{Write a template file}
\usage{
use_hot_file(
  name = NULL,
  filename = NULL,
  template = hotfun::project_template,
  open = interactive()
)

use_hot_gitignore(filename = NULL, template = NULL)

use_hot_readme(filename = NULL, template = NULL)
}
\arguments{
\item{name}{Name of file to write.  Not sure of the files available to you?
Run the function without specifying a name, and all files available within the
template will print.}

\item{filename}{Optional argument to specify the name of the file to be written.
Paths/filename is relative to project base (e.g. \code{here::here()})}

\item{template}{Project template.}

\item{open}{If \code{TRUE}, opens the new file.}
}
\description{
Rather than using \code{create_hot_project()} to start a new project folder, you
may use \code{use_hot_file()} to write a single file from any project template.
The functions \code{use_hot_gitignore()} and \code{use_hot_readme()} are shortcuts for
\code{use_hot_file("gitignore")} and \code{use_hot_file("readme")}.
}
\examples{
\donttest{\dontrun{
# create gitignore file
use_project_file("gitignore")
use_project_gitignore()

# create README.md file
use_project_file("readme")
use_project_readme()
}}
}
\seealso{
\code{\link[=create_hot_project]{create_hot_project()}}
}
